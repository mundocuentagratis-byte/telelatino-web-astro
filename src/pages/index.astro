---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import { appConfig } from "../data/app";

const movies = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

const news = (await getCollection("noticias"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

const featuredMovie = movies[0] || null;
const featuredNews = news[0] || null;

const formatDate = (date: Date) =>
  new Intl.DateTimeFormat("es-ES", {
    day: "2-digit",
    month: "short",
    year: "numeric",
  }).format(date);
---

<BaseLayout
  title={`${appConfig.name} | Canales en vivo, deportes y pelÃ­culas para Android`}
  description="Descarga TELELATINO para Android. Disfruta canales en vivo, deportes, películas y 7 días de prueba gratis."
>
  <main>
    <section class="hero-section">
      <div class="hero-bg"></div>

      <div class="container hero-grid">
        <div class="hero-copy">
          <span class="eyebrow">Disponible para Android</span>

          <h1>Canales en vivo, deportes y pelÃ­culas en una sola app.</h1>

          <p>
            {appConfig.name} es una app de entretenimiento para Android pensada
            para encontrar rÃ¡pido lo que mÃ¡s buscas: canales principales, eventos
            deportivos, pelÃ­culas y estrenos, sin contenido excesivo ni
            complicaciones.
          </p>

          <div class="hero-actions">
            <a href={appConfig.apkUrl} class="btn btn-primary" target="_blank" rel="noopener">
              Descargar APK
            </a>

            <a href={appConfig.whatsappUrl} class="btn btn-secondary" target="_blank" rel="noopener">
              WhatsApp
            </a>

            <a href={appConfig.telegramUrl} class="btn btn-secondary" target="_blank" rel="noopener">
              Telegram
            </a>
          </div>

          <div class="hero-stats">
            <div>
              <strong>{appConfig.trial.days} dÃ­as</strong>
              <span>Prueba gratis</span>
            </div>

            <div>
              <strong>{appConfig.vipPlan.price}</strong>
              <span>VIP por {appConfig.vipPlan.duration}</span>
            </div>

            <div>
              <strong>Android</strong>
              <span>App mÃ³vil</span>
            </div>
          </div>
        </div>

        <div class="hero-card" aria-label="PresentaciÃ³n de TELELATINO">
          <div class="phone-frame">
            <img src={appConfig.image} alt="TELELATINO para Android" />
            <h2>Entretenimiento fÃ¡cil desde tu mÃ³vil</h2>
            <p>
              Canales en vivo, deportes, pelÃ­culas y estrenos en una experiencia
              simple para Android.
            </p>
          </div>

          <ul class="hero-list">
            <li>Canales principales en vivo</li>
            <li>Eventos deportivos destacados</li>
            <li>PelÃ­culas y estrenos fÃ¡ciles de encontrar</li>
          </ul>
        </div>
      </div>
    </section>

    <section id="contenido" class="preview-section">
      <div class="container">
        <div class="section-heading">
          <span class="eyebrow">Contenido actualizado</span>
          <h2>PelÃ­culas y noticias destacadas.</h2>
          <p>
            Entra rÃ¡pido a las secciones principales: pelÃ­culas recientes y
            noticias deportivas. Cada tarjeta te lleva a la secciÃ³n completa, no
            a un solo artÃ­culo.
          </p>
        </div>

        <div class="preview-grid">
          <a class="preview-card movie-preview" href="/blog/" aria-label="Ver pelÃ­culas">
            <div class="preview-image">
              <img
                src={featuredMovie?.data.image || appConfig.image}
                alt={featuredMovie?.data.imageAlt || "PelÃ­culas en TELELATINO"}
              />
              <span>PelÃ­culas</span>
            </div>

            <div class="preview-content">
              <small>
                {featuredMovie ? formatDate(featuredMovie.data.pubDate) : "Actualizado"}
              </small>

              <h3>
                {featuredMovie?.data.title || "PelÃ­culas recientes y estrenos"}
              </h3>

              <p>
                {featuredMovie?.data.description ||
                  "Encuentra pelÃ­culas recientes, estrenos y recomendaciones para disfrutar desde Android."}
              </p>

              <strong>Ver pelÃ­culas</strong>
            </div>
          </a>

          <a class="preview-card news-preview" href="/noticias/" aria-label="Ver noticias">
            <div class="preview-image">
              <img
                src={featuredNews?.data.image || appConfig.image}
                alt={featuredNews?.data.imageAlt || "Noticias en TELELATINO"}
              />
              <span>Noticias</span>
            </div>

            <div class="preview-content">
              <small>
                {featuredNews ? formatDate(featuredNews.data.pubDate) : "Actualizado"}
              </small>

              <h3>
                {featuredNews?.data.title || "Noticias deportivas recientes"}
              </h3>

              <p>
                {featuredNews?.data.description ||
                  "Consulta noticias deportivas, eventos destacados y novedades del mundo del fÃºtbol."}
              </p>

              <strong>Ver noticias</strong>
            </div>
          </a>
        </div>
      </div>
    </section>

    <section id="beneficios" class="benefits-section">
      <div class="container">
        <div class="section-heading">
          <span class="eyebrow">Beneficios</span>
          <h2>Entretenimiento simple, rÃ¡pido y pensado para mÃ³viles.</h2>
          <p>
            TELELATINO estÃ¡ enfocada en lo que mÃ¡s se busca: canales principales,
            eventos deportivos, pelÃ­culas y estrenos, sin llenar la app de
            contenido innecesario.
          </p>
        </div>

        <div class="benefits-grid">
          {appConfig.benefits.map((benefit) => (
            <article class="benefit-card">
              <div class="benefit-icon">â–¶</div>
              <h3>{benefit.title}</h3>
              <p>{benefit.description}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="content-section">
      <div class="container content-grid">
        <div>
          <span class="eyebrow">Contenido destacado</span>
          <h2>Encuentra rÃ¡pido canales, deportes, pelÃ­culas y estrenos.</h2>
          <p>
            La experiencia de TELELATINO estÃ¡ pensada para usuarios que quieren
            abrir la app y encontrar lo importante sin perder tiempo. Todo estÃ¡
            orientado a mÃ³viles Android, con acceso a contenido popular y
            secciones fÃ¡ciles de usar.
          </p>

          <div class="feature-list">
            <div>
              <span>01</span>
              <strong>Canales principales en vivo</strong>
            </div>

            <div>
              <span>02</span>
              <strong>Eventos deportivos destacados</strong>
            </div>

            <div>
              <span>03</span>
              <strong>PelÃ­culas, estrenos y entretenimiento</strong>
            </div>
          </div>
        </div>

        <div class="info-card">
          <h3>DiseÃ±ada para Android</h3>

          <p>
            TELELATINO estÃ¡ disponible para dispositivos Android. Su objetivo es
            ofrecer una experiencia mÃ³vil sencilla, directa y enfocada en el
            entretenimiento que mÃ¡s buscan los usuarios.
          </p>

          <div class="info-row">
            <span>Plataforma</span>
            <strong>{appConfig.platform}</strong>
          </div>

          <div class="info-row">
            <span>Prueba gratis</span>
            <strong>{appConfig.trial.days} dÃ­as</strong>
          </div>

          <div class="info-row">
            <span>Plan VIP</span>
            <strong>{appConfig.vipPlan.price}</strong>
          </div>
        </div>
      </div>
    </section>

    <section id="planes" class="plans-section">
      <div class="container">
        <div class="section-heading">
          <span class="eyebrow">Planes</span>
          <h2>Empieza con prueba gratis y continÃºa con el plan VIP.</h2>
          <p>
            Al registrarte en TELELATINO puedes obtener {appConfig.trial.text}.
            DespuÃ©s tienes la opciÃ³n de acceder al plan VIP de {appConfig.vipPlan.duration}
            por solo {appConfig.vipPlan.price}.
          </p>
        </div>

        <div class="plan-card">
          <div>
            <span class="plan-badge">Plan VIP</span>
            <h3>{appConfig.vipPlan.text}</h3>
            <p>
              Una opciÃ³n econÃ³mica para seguir disfrutando canales en vivo,
              eventos deportivos, pelÃ­culas y estrenos desde tu mÃ³vil Android.
            </p>
          </div>

          <div class="plan-actions">
            <a href={appConfig.apkUrl} class="btn btn-primary" target="_blank" rel="noopener">
              Descargar APK
            </a>

            <a href={appConfig.whatsappUrl} class="btn btn-secondary" target="_blank" rel="noopener">
              Consultar por WhatsApp
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .container {
    width: min(1120px, calc(100% - 36px));
    margin: 0 auto;
  }

  .hero-section {
    position: relative;
    overflow: hidden;
    padding: 92px 0 76px;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 78% 18%, rgba(255, 197, 71, 0.18), transparent 30%),
      radial-gradient(circle at 20% 28%, rgba(46, 201, 255, 0.13), transparent 32%),
      linear-gradient(180deg, rgba(6, 27, 47, 0.35), rgba(4, 13, 28, 0.1));
    pointer-events: none;
  }

  .hero-grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 54px;
    align-items: center;
  }

  .eyebrow {
    display: inline-flex;
    width: fit-content;
    margin-bottom: 18px;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 197, 71, 0.2);
    color: #ffd77a;
    font-size: 0.78rem;
    font-weight: 900;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  h1,
  h2,
  h3,
  p {
    margin-top: 0;
  }

  .hero-copy h1 {
    max-width: 660px;
    margin-bottom: 20px;
    font-size: clamp(3.1rem, 7vw, 6.7rem);
    line-height: 0.9;
    letter-spacing: -0.085em;
  }

  .hero-copy p {
    max-width: 650px;
    color: rgba(255, 255, 255, 0.76);
    font-size: 1.03rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 28px;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 48px;
    padding: 13px 24px;
    border-radius: 999px;
    font-weight: 900;
    text-decoration: none;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      background 0.2s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .btn-primary {
    background: linear-gradient(180deg, #ffe47c 0%, #f3c747 100%);
    color: #171717;
    box-shadow: 0 14px 32px rgba(255, 198, 69, 0.28);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.16);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.18);
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    margin-top: 30px;
    max-width: 560px;
  }

  .hero-stats div {
    padding: 16px;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.16);
  }

  .hero-stats strong {
    display: block;
    color: #ffd66c;
    font-size: 1.25rem;
    line-height: 1.1;
  }

  .hero-stats span {
    display: block;
    margin-top: 4px;
    color: rgba(255, 255, 255, 0.72);
    font-size: 0.86rem;
  }

  .hero-card {
    padding: 24px;
    border-radius: 34px;
    background:
      radial-gradient(circle at top right, rgba(255, 216, 111, 0.2), transparent 30%),
      rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.16);
    box-shadow: 0 24px 80px rgba(0, 0, 0, 0.28);
  }

  .phone-frame {
    padding: 30px 24px;
    border-radius: 30px;
    background:
      linear-gradient(180deg, rgba(7, 31, 53, 0.35), rgba(4, 15, 31, 0.86)),
      radial-gradient(circle at 50% 0%, rgba(255, 197, 71, 0.22), transparent 42%);
    text-align: center;
  }

  .phone-frame img {
    width: 150px;
    height: 150px;
    border-radius: 34px;
    object-fit: cover;
    box-shadow: 0 20px 60px rgba(255, 198, 69, 0.24);
  }

  .phone-frame h2 {
    margin: 24px auto 10px;
    max-width: 360px;
    font-size: clamp(2rem, 4vw, 3.1rem);
    line-height: 0.98;
    letter-spacing: -0.055em;
  }

  .phone-frame p {
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.72);
  }

  .hero-list {
    display: grid;
    gap: 12px;
    margin: 18px 0 0;
    padding: 0;
    list-style: none;
  }

  .hero-list li {
    padding: 14px 16px;
    border-radius: 16px;
    background: rgba(111, 214, 255, 0.12);
    color: rgba(255, 255, 255, 0.88);
    font-weight: 800;
  }

  .preview-section,
  .benefits-section,
  .content-section,
  .plans-section {
    padding: 82px 0;
  }

  .preview-section {
    background:
      radial-gradient(circle at top left, rgba(255, 197, 71, 0.12), transparent 32%),
      rgba(255, 255, 255, 0.025);
  }

  .section-heading {
    max-width: 840px;
    margin: 0 auto 42px;
    text-align: center;
  }

  .section-heading h2 {
    margin-bottom: 16px;
    font-size: clamp(2.4rem, 5vw, 4.8rem);
    line-height: 0.98;
    letter-spacing: -0.065em;
  }

  .section-heading p {
    color: rgba(255, 255, 255, 0.76);
    font-size: 1.03rem;
    line-height: 1.7;
  }

  .preview-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 22px;
  }

  .preview-card {
    overflow: hidden;
    border-radius: 30px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.07));
    border: 1px solid rgba(255, 255, 255, 0.16);
    text-decoration: none;
    color: #ffffff;
    box-shadow: 0 22px 70px rgba(0, 0, 0, 0.2);
    transition:
      transform 0.2s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease;
  }

  .preview-card:hover {
    transform: translateY(-4px);
    border-color: rgba(255, 214, 108, 0.55);
    box-shadow: 0 28px 80px rgba(0, 0, 0, 0.28);
  }

  .preview-image {
    position: relative;
    height: 260px;
    overflow: hidden;
    background: rgba(255, 255, 255, 0.08);
  }

  .preview-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.82;
    transform: scale(1.02);
  }

  .preview-image::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 20%, rgba(5, 14, 29, 0.72));
  }

  .preview-image span {
    position: absolute;
    left: 18px;
    bottom: 18px;
    z-index: 2;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 215, 116, 0.92);
    color: #151515;
    font-size: 0.82rem;
    font-weight: 950;
  }

  .preview-content {
    padding: 26px;
  }

  .preview-content small {
    display: block;
    margin-bottom: 10px;
    color: #ffd978;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .preview-content h3 {
    margin-bottom: 12px;
    font-size: clamp(1.65rem, 3vw, 2.6rem);
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .preview-content p {
    color: rgba(255, 255, 255, 0.74);
    line-height: 1.65;
  }

  .preview-content strong {
    display: inline-flex;
    margin-top: 10px;
    color: #ffd978;
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 18px;
  }

  .benefit-card,
  .info-card,
  .plan-card {
    border-radius: 26px;
    background: rgba(255, 255, 255, 0.09);
    border: 1px solid rgba(255, 255, 255, 0.16);
  }

  .benefit-card {
    padding: 26px;
  }

  .benefit-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    margin-bottom: 22px;
    border-radius: 16px;
    background: rgba(255, 216, 111, 0.9);
    color: #121212;
    font-weight: 950;
  }

  .benefit-card h3 {
    margin-bottom: 12px;
    font-size: 1.25rem;
    letter-spacing: -0.03em;
  }

  .benefit-card p {
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.72);
    line-height: 1.6;
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 50px;
    align-items: center;
  }

  .content-grid h2 {
    max-width: 680px;
    margin-bottom: 18px;
    font-size: clamp(2.5rem, 5vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.065em;
  }

  .content-grid p {
    color: rgba(255, 255, 255, 0.76);
    line-height: 1.75;
  }

  .feature-list {
    display: grid;
    gap: 12px;
    margin-top: 28px;
  }

  .feature-list div,
  .info-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    padding: 16px 18px;
    border-radius: 16px;
    background: rgba(111, 214, 255, 0.12);
  }

  .feature-list span {
    color: #ffd978;
    font-weight: 950;
  }

  .feature-list strong,
  .info-row strong {
    color: #ffffff;
  }

  .info-card {
    padding: 30px;
  }

  .info-card h3 {
    font-size: 2rem;
    letter-spacing: -0.045em;
  }

  .info-row {
    margin-top: 12px;
  }

  .info-row span {
    color: rgba(255, 255, 255, 0.72);
    font-weight: 800;
  }

  .plans-section {
    background: rgba(255, 255, 255, 0.025);
  }

  .plan-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding: 34px;
  }

  .plan-badge {
    display: inline-flex;
    margin-bottom: 16px;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 216, 111, 0.85);
    color: #151515;
    font-weight: 950;
    font-size: 0.82rem;
  }

  .plan-card h3 {
    margin-bottom: 12px;
    font-size: clamp(1.8rem, 3vw, 3rem);
    letter-spacing: -0.05em;
  }

  .plan-card p {
    max-width: 680px;
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.74);
    line-height: 1.65;
  }

  .plan-actions {
    display: grid;
    gap: 12px;
    min-width: 240px;
  }

  @media (max-width: 900px) {
    .hero-grid,
    .preview-grid,
    .content-grid {
      grid-template-columns: 1fr;
    }

    .benefits-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .plan-card {
      align-items: flex-start;
      flex-direction: column;
    }

    .plan-actions {
      width: 100%;
    }
  }

  @media (max-width: 620px) {
    .hero-section {
      padding-top: 62px;
    }

    .hero-stats,
    .benefits-grid {
      grid-template-columns: 1fr;
    }

    .hero-actions {
      flex-direction: column;
    }

    .btn {
      width: 100%;
    }

    .preview-image {
      height: 220px;
    }
  }
</style>
