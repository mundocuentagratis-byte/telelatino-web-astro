---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const posts = await getCollection("noticias", ({ data }) => !data.draft);

const sortedPosts = posts.sort(
  (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
);

const fallbackImage = "/images/telelatino-app.jpg";
---

<BaseLayout
  title="Noticias | TELELATINO"
  description="Noticias recientes sobre deportes, fútbol, entretenimiento y novedades digitales relacionadas con TELELATINO."
>
  <section class="news-hero">
    <div class="container">
      <span class="badge">Noticias</span>

      <h1>Noticias recientes para estar al día</h1>

      <p>
        Actualidad deportiva, fútbol, novedades digitales y contenido relevante
        para usuarios que buscan entretenimiento desde Android.
      </p>
    </div>
  </section>

  <section class="news-section">
    <div class="container">
      {
        sortedPosts.length > 0 ? (
          <div class="news-grid">
            {sortedPosts.map((post) => (
              <article class="news-card">
                <a href={`/noticias/${post.id}/`} class="image-link">
                  <img
                    src={post.data.image || fallbackImage}
                    alt={post.data.imageAlt || post.data.title}
                    loading="lazy"
                  />
                </a>

                <div class="news-card-content">
                  <span class="category">{post.data.category}</span>

                  <h2>
                    <a href={`/noticias/${post.id}/`}>{post.data.title}</a>
                  </h2>

                  <p>{post.data.description}</p>

                  <div class="news-meta">
                    <span>
                      {post.data.pubDate.toLocaleDateString("es-ES", {
                        year: "numeric",
                        month: "long",
                        day: "numeric",
                      })}
                    </span>
                  </div>

                  <a href={`/noticias/${post.id}/`} class="read-more">
                    Leer noticia →
                  </a>
                </div>
              </article>
            ))}
          </div>
        ) : (
          <div class="empty-state card">
            <h2>Muy pronto habrá nuevas noticias</h2>
            <p>
              Estamos preparando contenido informativo relacionado con deportes,
              entretenimiento y TELELATINO.
            </p>
          </div>
        )
      }
    </div>
  </section>
</BaseLayout>

<style>
  .news-hero {
    padding: 90px 0 48px;
    text-align: center;
  }

  .news-hero h1 {
    max-width: 860px;
    margin: 24px auto 18px;
    font-size: clamp(2.5rem, 6vw, 5rem);
    line-height: 0.96;
    letter-spacing: -0.07em;
  }

  .news-hero p {
    max-width: 740px;
    margin: 0 auto;
    color: var(--color-muted);
    font-size: 1.08rem;
  }

  .news-section {
    padding: 30px 0 90px;
  }

  .news-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 26px;
  }

  .news-card {
    overflow: hidden;
    border-radius: 28px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.09), rgba(255, 255, 255, 0.045)),
      rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.11);
    box-shadow: 0 24px 80px rgba(0, 0, 0, 0.18);
    transition:
      transform 0.22s ease,
      border-color 0.22s ease,
      box-shadow 0.22s ease;
  }

  .news-card:hover {
    transform: translateY(-6px);
    border-color: rgba(255, 198, 41, 0.32);
    box-shadow: 0 30px 90px rgba(0, 0, 0, 0.28);
  }

  .image-link {
    display: block;
    position: relative;
    overflow: hidden;
    aspect-ratio: 16 / 9;
    background:
      radial-gradient(circle at top left, rgba(255, 198, 41, 0.28), transparent 42%),
      rgba(255, 255, 255, 0.06);
  }

  .image-link img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transform: scale(1.01);
    transition: transform 0.35s ease;
  }

  .news-card:hover .image-link img {
    transform: scale(1.06);
  }

  .news-card-content {
    padding: 24px;
  }

  .category {
    display: inline-flex;
    margin-bottom: 14px;
    color: var(--color-primary);
    font-size: 0.75rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .news-card h2 {
    margin: 0 0 12px;
    font-size: 1.45rem;
    line-height: 1.08;
    letter-spacing: -0.04em;
  }

  .news-card h2 a {
    color: var(--color-text);
    text-decoration: none;
  }

  .news-card p {
    margin: 0;
    color: var(--color-muted);
    font-size: 0.98rem;
  }

  .news-meta {
    margin-top: 18px;
    color: var(--color-muted);
    font-size: 0.88rem;
    font-weight: 700;
  }

  .read-more {
    display: inline-flex;
    margin-top: 20px;
    color: var(--color-primary);
    font-weight: 900;
    text-decoration: none;
  }

  .empty-state {
    max-width: 760px;
    margin: 0 auto;
    padding: 44px;
    text-align: center;
  }

  .empty-state h2 {
    margin: 0 0 12px;
    font-size: 2rem;
  }

  .empty-state p {
    margin: 0;
    color: var(--color-muted);
  }

  @media (max-width: 980px) {
    .news-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .news-hero {
      padding-top: 70px;
    }

    .news-grid {
      grid-template-columns: 1fr;
    }

    .news-card-content {
      padding: 22px;
    }
  }
</style>