<section class="hero" id="inicio">
  <div class="container hero-grid">
    <div class="hero-content">
      <span class="badge">Entretenimiento digital en un solo lugar</span>

      <h1>
        Disfruta televisión, deportes y contenido online con una experiencia moderna.
      </h1>

      <p>
        TeleLatino Oficial es un portal informativo y promocional diseñado para que los
        usuarios conozcan opciones de entretenimiento, novedades, guías y noticias
        relacionadas con contenido digital.
      </p>

      <div class="hero-actions">
        <a href="#descargar" class="btn btn-primary">
          Conocer la app
        </a>

        <a href="/noticias/" class="btn btn-secondary">
          Ver noticias
        </a>
      </div>

      <div class="hero-stats" aria-label="Beneficios destacados">
        <div>
          <strong>24/7</strong>
          <span>Contenido disponible</span>
        </div>

        <div>
          <strong>SEO</strong>
          <span>Noticias y guías</span>
        </div>

        <div>
          <strong>Web</strong>
          <span>Rápida y moderna</span>
        </div>
      </div>
    </div>

    <div class="hero-preview card">
      <div class="preview-top">
        <span></span>
        <span></span>
        <span></span>
      </div>

      <div class="screen">
        <div class="play-circle">▶</div>

        <div class="screen-content">
          <span>TeleLatino Oficial</span>
          <h2>TV, deportes y entretenimiento</h2>
          <p>Una experiencia visual optimizada para informar, promocionar y posicionar.</p>
        </div>
      </div>

      <div class="preview-list">
        <div>
          <span class="dot"></span>
          <p>Guías para usuarios</p>
        </div>

        <div>
          <span class="dot"></span>
          <p>Noticias de deportes</p>
        </div>

        <div>
          <span class="dot"></span>
          <p>Contenido optimizado para Google</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 96px 0 80px;
    overflow: hidden;
  }

  .hero::after {
    content: "";
    position: absolute;
    right: -160px;
    top: 120px;
    width: 420px;
    height: 420px;
    border-radius: 999px;
    background: rgba(255, 138, 0, 0.16);
    filter: blur(80px);
    z-index: -1;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 54px;
    align-items: center;
  }

  .hero-content h1 {
    margin: 22px 0 20px;
    max-width: 760px;
    font-size: clamp(2.7rem, 6vw, 5.4rem);
    line-height: 0.96;
    letter-spacing: -0.075em;
  }

  .hero-content p {
    max-width: 660px;
    margin: 0;
    color: var(--color-muted);
    font-size: 1.12rem;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 34px;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 14px;
    max-width: 660px;
    margin-top: 40px;
  }

  .hero-stats div {
    padding: 18px;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .hero-stats strong {
    display: block;
    color: var(--color-primary);
    font-size: 1.45rem;
    line-height: 1;
  }

  .hero-stats span {
    display: block;
    margin-top: 8px;
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  .hero-preview {
    position: relative;
    padding: 16px;
    border-radius: var(--radius-lg);
    transform: rotate(1deg);
  }

  .preview-top {
    display: flex;
    gap: 8px;
    padding: 4px 4px 14px;
  }

  .preview-top span {
    width: 11px;
    height: 11px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.25);
  }

  .screen {
    min-height: 390px;
    position: relative;
    display: flex;
    align-items: end;
    padding: 26px;
    overflow: hidden;
    border-radius: 24px;
    background:
      linear-gradient(to top, rgba(0, 0, 0, 0.82), transparent 58%),
      radial-gradient(circle at 75% 20%, rgba(255, 138, 0, 0.72), transparent 22%),
      linear-gradient(135deg, #123a65, #07111f 64%);
  }

  .screen::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      linear-gradient(120deg, rgba(255, 255, 255, 0.16), transparent 36%),
      radial-gradient(circle at 20% 20%, rgba(0, 212, 255, 0.24), transparent 28%);
  }

  .play-circle {
    position: absolute;
    top: 28px;
    right: 28px;
    width: 66px;
    height: 66px;
    display: grid;
    place-items: center;
    border-radius: 999px;
    background: linear-gradient(135deg, var(--color-primary), #ffb000);
    color: #1a1204;
    font-weight: 900;
    box-shadow: 0 20px 48px rgba(255, 138, 0, 0.34);
  }

  .screen-content {
    position: relative;
    z-index: 1;
  }

  .screen-content span {
    color: #ffd8a8;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.78rem;
  }

  .screen-content h2 {
    max-width: 360px;
    margin: 10px 0;
    font-size: clamp(1.9rem, 4vw, 3rem);
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .screen-content p {
    max-width: 380px;
    margin: 0;
    color: rgba(255, 255, 255, 0.74);
  }

  .preview-list {
    display: grid;
    gap: 12px;
    margin-top: 16px;
  }

  .preview-list div {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.06);
  }

  .preview-list p {
    margin: 0;
    color: var(--color-muted);
    font-weight: 700;
    font-size: 0.93rem;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: var(--color-success);
    box-shadow: 0 0 0 6px rgba(50, 213, 131, 0.12);
  }

  @media (max-width: 980px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-preview {
      transform: none;
    }
  }

  @media (max-width: 680px) {
    .hero {
      padding: 70px 0 56px;
    }

    .hero-content h1 {
      letter-spacing: -0.055em;
    }

    .hero-stats {
      grid-template-columns: 1fr;
    }

    .screen {
      min-height: 330px;
      padding: 22px;
    }

    .hero-actions {
      flex-direction: column;
    }
  }
</style>